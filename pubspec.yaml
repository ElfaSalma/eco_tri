name: eco_tri
description: Une application de tri intelligent avec Flutter et IA
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  tflite: ^1.1.2
  image_picker: ^1.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/model_unquant.tflite
    - assets/labels.txt
